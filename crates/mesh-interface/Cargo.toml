[package]
name = "mesh-interface"
version = "0.1.0"
edition = "2021"
description = "Minimal versioned Agent-X mesh/capability boundary"

[dependencies]
serde = { version = "1", features = ["derive"] }
