# Console patch v5

Source: `ava007-console-patch-v5.zip`

Contains Agent Browser platform components:

- `platform/src/components/shell/*` (DevExoskelCard, TelemetryShare, GSAP hooks)
- `platform/src/components/viewport/Viewport.tsx`
- `platform/src/app/console/ava-shell.css`
- `APPLY.md` — merge instructions

Expand the zip on the Agent Browser tree and follow APPLY.md. Not auto-merged into `modules/` to avoid clobbering live UI.
