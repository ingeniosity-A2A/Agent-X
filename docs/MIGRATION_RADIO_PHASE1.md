# Phase 1 — Radio fracture fix (inventory, no deletes)

1. Lock ownership table in `docs/RADIO_OWNERSHIP.md`.
2. Create `containers/*` skeletons (this PR).
3. Inventory paths still mixed in QAG-MemBrain `skills/telecom/` hardware drivers.
4. Keep all existing code until Phase 5 tests pass.
5. Do **not** delete Onomondo/Telnyx/SDR code from MemBrain or Ava007 until shims land.

Next: Phase 2 move implementations behind A2A capability contracts.
