# ESA (Extended Stay America) service — AgentMail
ESA_AVA_EMAIL=ava007@agentmail.to
ESA_MANAGER_EMAIL=bmccray02@gmail.com
AGENTMAIL_API_KEY=
AGENTMAIL_INBOX_ID=ava007@agentmail.to

# Cloudflare R2 (S3-compatible)
# Endpoint only — put ACCESS_KEY_ID / SECRET in .env.local, never commit
R2_S3_ENDPOINT=https://fb298783502db03eb74a63b9b6b6f69f.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PUBLIC_URL=

# Hugging Face Hub Substrate (doctrine: Ava007_Permanent_Master_Skill_HuggingFace.md)
# Token with inference + repo read scope — https://huggingface.co/settings/tokens
HF_TOKEN=

# Mercury2 — Cortex tier, diffusion + reasoning LLM (canonical env prefix: MERCURY2_*)
# Key from https://dashboard.inceptionlabs.ai/ — output arrives as a BLOCK, not a stream
# Model `mercury-2` verified live 2026-09-09; mercury-coder-small is gated for
# accounts created after Feb 24, 2026 — do not revert.
MERCURY2_API_KEY=
# MERCURY2_ENDPOINT=https://api.inceptionlabs.ai/v1/chat/completions
# MERCURY2_MODEL=mercury-2

# ── Persistence (Prisma + SQLite) ─────────────────────────────────────────────
DATABASE_URL="file:./db/custom.db"

# ── ESA site surface ──────────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=https://esa.ingeniosity.tech
ESA_AVA_INBOX_ID=ava007@agentmail.to

# ── RocksDB render-stack engine (substrate) ──────────────────────────────────
ROCKS_PY=python3

# ═══════════════════════════════════════════════════════════════════════════════
# WHY YOUR .ENV "DISAPPEARS": real secrets live in `.env.local`, which is
# GITIGNORED BY DESIGN (secrets must never enter git) and this sandbox does not
# persist untracked files between sessions. The contract above IS the durable
# copy — versioned in git. Fill your real keys in `platform/.env.local` each
# session, or inject them via the dashboard sources (Interface ▸ env-vars UI).
# ═══════════════════════════════════════════════════════════════════════════════
