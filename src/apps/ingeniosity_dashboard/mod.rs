//! RETIRED — ingeniosity_dashboard placeholder.
//!
//! Agent-X does not ship a general dashboard. ESA operator surface is the
//! platform ESA Exoskeleton (`/consoles/esa-maintenance`) with Select Card.

pub fn routes() -> axum::Router {
    axum::Router::new()
}
